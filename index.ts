class DeonayClient {
  private apiKey: string;

  constructor(apiKey: string) {
    this.apiKey = apiKey;
  }

  users = {
    list() {
      
    },

    getById() {

    },
  }
}

export default DeonayClient;